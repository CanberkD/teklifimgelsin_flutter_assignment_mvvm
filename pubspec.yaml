name: teklifimgelsin_flutter_assignment_mvvm
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  #HTTP request package.
  dio: ^5.3.2

  #StateManagment
  flutter_mobx: ^2.0.6+5
  mobx: ^2.1.3
  provider: ^6.0.5
  build_runner: ^2.3.3

  #Miscellaneous
  dots_indicator: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

  #StateManagment
  mobx_codegen: ^2.1.1

flutter:
  uses-material-design: true
